# Local .terraform directories
**/.terraform/*

# Terraform lockfile
.terraform.lock.hcl

# Terraform state files
*.tfstate
*.tfstate.*

# Terraform plan files
*.tfplan

# Crash log files
crash.log

# Ignore all .tfvars files containing sensitive data (keys, secrets)
*.tfvars

# Override files used locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI config files
.terraformrc
terraform.rc

# Ignore SSH private keys (if accidentally added)
id_rsa
id_rsa.pem
*.pem
*.key

# Ignore SSH public keys (if accidentally added)
id_rsa.pub